import Swiper from 'swiper';

var swiper = new Swiper('.swiper-container', {
	autoHeight: true,
    speed: 600,
    pagination: {
        el: '.swiper-pagination',
        clickable: true
    }
});


